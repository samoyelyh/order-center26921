# 前端环境变量示例（复制为 .env.local 后按需修改）

# ---------------------------------------------------------------------------
# 后端 API 根地址
#
# 【重要】默认不要设置这个变量！
#   留空时前端会跟随「当前访问页面的主机名」，只替换端口：
#     本机访问    http://127.0.0.1:3000        → http://127.0.0.1:8000/api
#     局域网访问  http://192.168.0.31:3000     → http://192.168.0.31:8000/api
#   这样同事用自己的电脑打开局域网地址时，请求会打到**这台后端机器**。
#
#   如果这里硬编码 http://127.0.0.1:8000/api，局域网用户的 127.0.0.1
#   指的是他自己的电脑，页面必然报 Failed to fetch。
# ---------------------------------------------------------------------------
# VITE_MATERIAL_API_BASE=http://192.168.0.31:8000/api

# 后端端口（默认 8000；仅在端口不是 8000 时需要设置）
# VITE_MATERIAL_API_PORT=8000

# 是否走后端真实 API
#   1（默认）= 上传页真实调用后端
#   0         = 回退到本地内存 Mock（无后端时看 UI 用）
VITE_MATERIAL_API=1
